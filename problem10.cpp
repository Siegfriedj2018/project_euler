#include <iostream>
#include <cmath>
using std::cout;
using std::endl;

int main() {
    int SumPrimes = 17;
    int maxValue = 2000000;
    for (size_t i = 7; i <= maxValue; i += 1) {
        for (size_t j = 0; j <= sqrt(i); ++j) {
            if (fmod(i, j) == 0) {
                
            }
        }
    }
    // 104743
    
    // cout << __cplusplus << endl;
}
